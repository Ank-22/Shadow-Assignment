## Frontend UI

Static UI that posts images to the backend server.

Mask is optional; if not provided, the server uses the foreground alpha channel.

### Run

```bash
python backend/server.py
```

Then open:

- http://localhost:8000
